name: python tests niffler

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  python_tests:
    runs-on: ubuntu-latest
    env:
      FRONTEND_URL: http://127.0.0.1
      GATEWAY_URL: http://127.0.0.1:8090
      AUTH_URL: http://127.0.0.1:9000
      AUTH_SECRET: niffler:client
      SPEND_DB_URL: postgresql+psycopg2://postgres:secret@127.0.0.1:5432/niffler-spend
      USERDATA_DB_URL: postgresql+psycopg2://postgres:secret@127.0.0.1:5432/niffler-userdata
      TEST_USERNAME: qa_guru
      TEST_PASSWORD: qa_guru
      KAFKA_ADDRESS: localhost
      SOAP_ADDRESS: http://localhost:8089/ws
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ env.GITHUB_SHA }}
#      - name: Set up JDK 21
#        uses: actions/setup-java@v4
#        with:
#            java-version: '21'
#            distribution: 'temurin'
#      - name: Build backends
#        run: |
#          bash docker-compose-dev.sh
#      - name: Get the last commit message
#        run: |
#          echo "HEAD_COMMIT_MESSAGE=$(git show -s --format=%s)" >> $GITHUB_ENV
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'
      - name: Go to working directory
        run: cd python_test/ && ls -l
#      - name: Install dependencies
#        run: echo pwd && pip install --upgrade -r requirements.txt


#      - name: Run e2e tests
#        id: e2e
#        run: |
#          docker compose up -d
#          docker ps -a
#          docker wait niffler-e-2-e
#          exit_code=$$(docker inspect -f '{{.State.ExitCode}}' niffler-e-2-e)
#          echo "E2E_EXIT_CODE=$exit_code" >> $GITHUB_OUTPUT
#          echo "### Test logs ###"
#          docker logs niffler-e-2-e
#          if [ "$exit_code" -eq "0"]; then
#            echo "Test passed successfully!"
#            exit 0
#          else
#            echo "Tests failed!"
#            exit 1
#          fi
#      - name: Add comment to PR with link to allure
#        if: always()
#        uses: action/github-script@v7
#        with:
#          script: |
#            const exitCode = ${{ steps.e2e.outputs.E2E_EXIT_CODE }}
#            const reportUrl = 'https://allure.niffler-stage.qa.guru/api/allure-docker-service/projects/niffler-ng/reports/latest/index.html'
#            const historyUrl = 'https://allure.niffler-stage.qa.guru/allure-docker-service-ui/projects/niffler-ng'
#            cons message = exitCode == '0' ?
